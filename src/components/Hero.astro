---
import { siteData as hospital } from '../lib/site-data.js'
---

<section class="hero">
  <h1>{hospital.hero.title}</h1>
  <p>{hospital.hero.subtitle}</p>
  <a href="/contact" class="btn">{hospital.cta.label}</a>
</section>
